name: docs

on: [pull_request]

jobs:
  docs-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: promptlylabs/docs-framework/.github/actions/docs-check@v0.7.0  # x-release-please-version
